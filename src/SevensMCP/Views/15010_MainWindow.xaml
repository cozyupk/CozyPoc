<Window x:Class="CozyPoC.SevensMCP.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SevensMCP" Width="1000" Height="500">
    <DockPanel LastChildFill="true">
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" HorizontalAlignment="Right"
                    DataContext="{Binding DevToolViewModel}">
            <Button Width="200" Margin="5" Command="{Binding ThrowUIExceptionCommand}">DispatcherUnhandledException</Button>
            <Button Width="200" Margin="5" Command="{Binding ThrowTaskExceptionCommand}">UnobservedTaskException</Button>
            <Button Width="200" Margin="5" Command="{Binding ThrowBackgroundTaskExceptionCommand}">UnhandledException</Button>
        </StackPanel>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Cards}">
                <ItemsControl.ItemsPanel>
                    <!-- 13列×4行の均等グリッド -->
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="13" Rows="4"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Margin" Value="1"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="2" Padding="1" BorderThickness="1" BorderBrush="#33000000"
                            Background="Transparent">
                            <Border.InputBindings>
                                <MouseBinding MouseAction="LeftClick"
                                    Command="{Binding ClickedCommand}" />
                            </Border.InputBindings>

                            <Image Source="{Binding FilePath}"
                                Stretch="Uniform" Width="53" Height="77"
                                SnapsToDevicePixels="True"
                                Visibility="{Binding Visibility}"
                                IsHitTestVisible="False" />
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</Window>